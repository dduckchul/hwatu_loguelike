# 에이전트 작업 지침

이 저장소에서 작업하는 모든 코딩 에이전트는 아래 규칙을 따른다.

## 작업 시작 전

1. 이 문서와 작업 대상 경로에 더 가까운 `AGENTS.md`를 모두 읽는다.
2. `docs/GAMEPLAY.md`, `docs/ARCHITECTURE.md`, `docs/DATA_SCHEMA.md`에서 작업과 관련된 부분을 읽는다.
3. `docs/IMPLEMENTATION_PLAN.md`에서 현재 마일스톤과 완료 조건을 확인한다.
4. 기존 변경 사항을 확인하고, 사용자의 작업을 덮어쓰거나 되돌리지 않는다.
5. 코드를 수정하기 전에 구현 목표, 수정할 파일 또는 폴더, 이번 작업에서 하지 않을 범위를 사용자에게 간략히 설명한다.
6. 사용자가 제안한 범위에 대해 명시적으로 승인한 뒤에만 코드와 에셋을 수정한다.
7. 승인받은 범위를 넘어설 필요가 생기면 작업을 멈추고 변경된 범위를 다시 설명한 뒤 승인을 받는다.

## 프로젝트 환경

- Unity `6000.3.21f1`을 사용한다.
- 2D 프로젝트이며 Universal Render Pipeline을 사용한다.
- 첫 대상 플랫폼은 WebGL이다.
- 에이전트는 `.meta` 파일을 직접 생성하거나 수정하지 않는다. 새 `.meta` 파일은 Unity 에디터가 생성하도록 한다.
- 이미 존재하는 `.meta` 파일은 삭제하거나 에셋과 분리하지 않는다.
- 사용자가 승인한 범위에서는 Unity MCP를 사용해 GameObject와 컴포넌트를 생성하고, 직렬화 필드를 할당하고, 씬과 프리팹을 연결할 수 있다.
- Unity MCP로 안전하게 처리하지 못한 Inspector 연결은 사용자가 직접 수행하며, 필요한 GameObject, 컴포넌트, Inspector 필드와 연결 절차를 완료 보고에 명시한다.
- 버튼 `OnClick` 같은 복잡한 Inspector 이벤트는 전용 도구로 안전하게 설정하고 검증할 수 있기 전까지 사용자가 직접 연결한다.
- 에이전트는 씬 연결을 위해 `.unity`, `.prefab`, `.meta` 파일의 YAML을 직접 수정하지 않는다.
- `Library`, `Temp`, `Logs`, `Obj`, `UserSettings` 등 생성 폴더를 소스에 추가하지 않는다.

## Unity MCP와 에디터 작업

- Unity 에디터 작업이 필요한 경우 먼저 MCP로 에디터 상태, 컴파일 여부, 열린 씬과 저장되지 않은 변경을 확인한다.
- 씬이나 프리팹을 수정하기 전에 대상과 변경 내용을 설명하고 사용자의 명시적인 승인을 받는다.
- 예상하지 못한 저장되지 않은 변경이 있는 씬이나 프리팹은 수정하거나 저장하지 않고 사용자에게 보고한다.
- 전용 MCP 명령이 있으면 범용 `eval`, 스크립트 실행, 메뉴 실행보다 우선한다.
- `eval`로 프로젝트나 에디터 상태를 변경하는 작업은 전용 명령으로 처리할 수 없고 사용자가 해당 방식을 승인한 경우에만 수행한다.
- 승인받아 수정한 대상만 저장하며, 관련 없는 열린 씬이나 에셋을 함께 저장하지 않는다.
- Unity CLI는 에디터와 Pipeline 상태 확인, 빌드, 테스트처럼 CLI에 적합한 작업에 사용하고, 씬과 에셋 작성은 Unity MCP의 전용 명령을 우선한다.

## 기획과 문서

- 기획 문서는 사용자가 명시적으로 요청한 경우에만 수정한다.
- `확정` 규칙만 구현한다. `실험`, `미정`, `보류` 항목을 임의로 확정하지 않는다.
- 구현에 필요한 규칙이 미정이면 해당 부분을 임의로 구현하지 말고, 확정된 범위만 작업한 뒤 미정 사항을 보고한다.
- 기획 변경이 필요해 보이면 코드로 우회하지 말고 변경 이유와 선택지를 보고한다.
- 수치 밸런스와 콘텐츠 값은 가능한 한 데이터로 관리하고 로직에 흩어 넣지 않는다.

## 아키텍처 원칙

- 코드는 `Cards`, `Deck`, `Hands`, `Combat`, `Rewards`, `Randomness`, `UI` 기능별 폴더로 나눈다.
- 족보 판정, 피해 계산과 덱 순환 같은 핵심 규칙은 각 기능 폴더 안의 일반 C# 규칙 코드로 구현한다.
- UI는 계산 결과를 표시하고 입력을 전달한다. UI에서 족보나 피해를 다시 계산하지 않는다.
- 난수는 외부에서 시드 또는 난수 공급자를 주입할 수 있어야 한다.
- 카드와 적처럼 직접 참조하는 원본 데이터는 안정적인 고유 ID를 사용한다.
- 핵심 전투 규칙은 `MonoBehaviour`, `ScriptableObject`, `GameObject`에 직접 의존하지 않는 C# 코드로 유지한다.
- `MonoBehaviour`는 입력, 표시, 생명주기 연결을 담당하고 규칙 계산을 소유하지 않는다.
- `ScriptableObject`는 콘텐츠 작성과 로딩 경계로 사용하며 런 중 변경 상태를 직접 저장하지 않는다.
- 새 시스템을 추가할 때 기존 공개 인터페이스를 우회하는 중복 경로를 만들지 않는다.

## 구현 범위

- 한 작업은 하나의 검증 가능한 목표에 집중한다.
- 요청에 없는 대규모 리팩터링, 새 의존성, 새 게임 규칙, 최종 아트 제작은 하지 않는다.
- 저장 기능, 자동화 테스트, 범용 프레임워크는 현재 제출 범위에 포함하지 않는다.
- 플레이스홀더는 명확히 표시하고 최종 자산처럼 취급하지 않는다.
- 비밀값, 개인 경로, 로컬 전용 설정을 저장소에 추가하지 않는다.

## 검증

- 변경한 기능을 Unity 에디터에서 직접 실행해 정상 흐름과 관련 경계 상황을 수동으로 확인한다.
- 검증 전후로 Unity Console의 오류와 예외를 확인해 새로 발생한 문제를 구분한다.
- MCP로 수행한 검증 결과와 사용자가 직접 확인해야 하는 시각적·조작감 검증을 구분해 보고한다.
- 빌드에 영향을 주는 변경이거나 제출 전 검증이라면 WebGL 빌드를 확인한다.
- 검증하지 못한 항목은 통과했다고 표현하지 말고 이유와 영향을 보고한다.

## 완료 보고

다음을 간결하게 보고한다.

- 구현하거나 변경한 내용
- 변경한 주요 파일
- 실행한 수동 검증과 결과
- 남은 미정 사항 또는 위험
